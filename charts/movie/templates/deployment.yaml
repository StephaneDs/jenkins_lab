apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "movie.fullname" . }}
  labels:
    {{- include "movie.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "movie.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "movie.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
          env:
            - name: DB_HOST
              value: "movie-db-postgresql.{{ .Release.Namespace }}.svc.cluster.local"

            - name: DATABASE_URI
              valueFrom:
                secretKeyRef:
                  name: {{ printf "%s-%s" .Values.db.secretName .Release.Namespace }}
                  key: {{ .Values.db.secretKey }}

            - name: CAST_SERVICE_HOST_URL
              value: "http://cast.{{ .Release.Namespace }}.svc.cluster.local:8000/api/v1/casts/"

            - name: LOG_LEVEL
              value: "{{ .Values.env.LOG_LEVEL | default "INFO" }}"    

          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}

